---
inclusion: fileMatch
fileMatchPattern: "docs/**"
---

# Markdown tables

Keep GFM pipe tables spaced and aligned (`| ---- | ---- |`). Escape literal pipes inside cells
as `\|`. The `format-markdown-tables` hook repairs dirty files on save.

Prefer simple tables over giant matrices; put scaling detail on Cost, not Patterns.
